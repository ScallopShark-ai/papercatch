name: arxiv_tracker
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - arxiv>=2.1.0
      - google-genai>=1.0.0
      - requests>=2.31.0
      - openai>=1.0.0
      
